# Calculating Angles using Trigonometry
The goal is:
1. Avoid floating point math
2. Avoid using `math.atan2` and `math.sqrt` as these allocate memory

[This is the old way](./read_test.py) and [this is the new way](./read_test2.py), using trigonometry approximation and integer math!